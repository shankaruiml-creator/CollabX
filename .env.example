# CollabX Environment Variables Template
# Copy this file and fill in your actual values
# NEVER commit the actual .env file to GitHub

# Server Configuration
PORT=8080

# Database Configuration (Use managed MySQL service)
DATABASE_URL=jdbc:mysql://your-db-host:3306/collabx?useSSL=true&requireSSL=true&serverTimezone=UTC
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# JWT Configuration (Generate with: openssl rand -base64 64)
JWT_SECRET=your_jwt_secret_key_minimum_256_bits_long

# Email Configuration (Use Gmail App Password)
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_specific_password

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-domain.vercel.app
